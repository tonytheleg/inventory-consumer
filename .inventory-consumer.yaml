consumer:
  enabled: true
  bootstrap-servers: localhost:9092
  retry-options:
    consumer-max-retries: 3
    operation-max-retries: 4
    backoff-factor: 5
  auth:
    enabled: false
client:
  enabled: false
  url: "localhost:8000"
  enable-oidc-auth: false
  insecure: true
log:
  level: "debug"
